apiVersion: 1

providers:
  - name: MonitoringProvider
    orgId: 1
    folder: MonitoringDir
    # folderUid: monitoring
    type: file
    disableDeletion: false
    updateIntervalSeconds: 60
    allowUiUpdates: true
    options:
      path: /etc/dashboards
      # foldersFromFilesStructure: false
